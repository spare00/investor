# Closing Policy

`ClosingService` applies Phase 3 `ClosingPolicyEngine` to live `position_lifecycles`, produces intent drafts, blocks new entries in closing window when configured. Does not auto-submit broker orders under default settings.
